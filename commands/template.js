const Discord = require("discord.js");
const config = require("../config.json");
const fs = require("fs");

module.exports = {
    name: 'template',
    description: 'Template!',
    permissions: 'PERMISSION',
    example: '',
    async execute(bot, message, args, con) {
        //HER SKAL DIN COMMAND VÆRE
    },
};